// JavaScript for popup
document.addEventListener("DOMContentLoaded", function () {
    const popup = document.querySelector(".popup");
    // Show popup on recommendation submission
    // Add event listeners and functions
});
